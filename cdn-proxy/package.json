{
  "name": "sanity-cdn-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies cdn.sanity.io with 1-year edge cache to minimize Sanity bandwidth usage",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250116.0"
  }
}
